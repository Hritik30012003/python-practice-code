from basic.hello_chai import chai
         
chai("ginger tea")

# NOTES
#1 compile to byte code (low level and platform independent)
#byte code runs faster

# .pyc (compiled pythan) frozen binaries
# __pycache_
 
#source change and pythan virson
# (pythan.cpythan-310.pyc (works only for imported files not for top level filesss)

# pythan virtual machin PVM
# Code loop to iterate byte code
# Run time engine
#Also known as pythan interpreter

# Byte code is not the machin code
 
# Pythan code interpretation
# cpythan, jythan, Iron python,Stackless, PyPy
#(cpythan (standard implementation))
import hello_chai
from importlib import reload
reload (hello_chai)


from importlib import reload
reload(hello_chai)